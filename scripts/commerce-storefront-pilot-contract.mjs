import assert from "node:assert/strict";
import fs from "node:fs";

const pilot=fs.readFileSync("app/lib/aruter/storefrontPilot.ts","utf8");
const publicProducts=fs.readFileSync("app/api/aruter/public-products/route.ts","utf8");
const reservations=fs.readFileSync("app/api/aruter/public-reservations/route.ts","utf8");
const reservationStatus=fs.readFileSync("app/api/aruter/public-reservations/[reservationId]/status/route.ts","utf8");
const productRoute=fs.readFileSync("app/api/aruter/products/route.ts","utf8");
const orderRoute=fs.readFileSync("app/api/aruter/orders/route.ts","utf8");
const eventsRoute=fs.readFileSync("app/api/aruter/events/route.ts","utf8");
const databaseRepo=fs.readFileSync("app/lib/aruter/databaseRepository.ts","utf8");
const types=fs.readFileSync("app/lib/aruter/types.ts","utf8");
const ui=fs.readFileSync("components/aruter/AruterOfferPages.tsx","utf8");
const envExample=fs.readFileSync("app/lib/aruter/aruter-env.example","utf8");
const checks=[];
function check(name,condition){checks.push({name,condition:Boolean(condition)});console.log(`${condition?"PASS":"FAIL"} ${String(checks.length).padStart(2,"0")} ${name}`);}

check("storefront pilot flag is explicit opt-in",pilot.includes('ARUTER_STOREFRONT_PILOT_ENABLED')&&pilot.includes('=== "1"'));
check("cashier bridge flag is independently explicit opt-in",pilot.includes('ARUTER_STOREFRONT_ORDER_BRIDGE_ENABLED')&&pilot.includes('=== "1"'));
check("pilot catalog remains empty when pilot flag is disabled",pilot.includes('pilotEnabled: false')&&pilot.includes('products: []'));
check("pilot catalog reads the shared Aruter repository",pilot.includes('getAruterRepository().listProducts()'));
check("catalog only publishes active products",pilot.includes('.filter((product) => product.isActive)'));
check("catalog respects template scope",pilot.includes('product.template === template'));
check("catalog respects public-offer visibility",pilot.includes('product.isPublicOffer !== false'));
check("public price is calculated from repository net price and VAT",pilot.includes('product.priceNet * (1 + product.vatRate / 100)'));
check("stock status is derived from repository stock",pilot.includes('product.stockQuantity <= 0')&&pilot.includes('product.stockQuantity < 10'));
check("reservation normalization resolves authoritative product by id",pilot.includes('item.id === productId')&&pilot.includes('normalizeStorefrontReservationInput'));
check("reservation normalization overwrites client product name",pilot.includes('name: product.name'));
check("reservation normalization overwrites client product price",pilot.includes('price: grossPrice(product)'));
check("reservation normalization overwrites client product unit",pilot.includes('unit: product.unit'));
check("reservation rejects unavailable product",pilot.includes('A kiválasztott termék nem érhető el a nyilvános ajánlatban.'));
check("reservation rejects quantity above current stock",pilot.includes('quantity > product.stockQuantity'));
check("cashier bridge uses stable public reservation marker",pilot.includes('[PUBLIC_RESERVATION:${reservationId}]'));
check("cashier bridge searches existing order before creating",pilot.indexOf('findOrderForReservation(reservation.id)')<pilot.indexOf('getAruterRepository().createOrder'));
check("reused bridge order is not duplicated",pilot.includes('event: "REUSED"')||pilot.includes('logBridge("REUSED"'));
check("bridge creates normal legacy cashier order",pilot.includes('getAruterRepository().createOrder')&&pilot.includes('customerType: "walk_in"')&&pilot.includes('recorderName: "Nyilvános Árutér"'));
check("bridge order uses authoritative SKU",pilot.includes('sku: product.sku'));
check("bridge order uses authoritative net price",pilot.includes('priceNet: product.priceNet'));
check("bridge order uses authoritative VAT rate",pilot.includes('vatRate: product.vatRate'));
check("bridge order uses authoritative storage zone",pilot.includes('storageZone: product.storageZone'));
check("bridge enqueues Commerce through trusted service queue",pilot.includes("queueStorefrontCommerceMirrorFailOpen(reservation.businessSlug, created.data)"));
check("bridge errors remain fail-open",pilot.includes('FAILED_FAIL_OPEN')&&pilot.includes('bridged: false'));
check("public reservation is persisted before bridge after callback",reservations.indexOf('createPublicReservation(normalized.input)')<reservations.indexOf('after(async ()'));
check("reservation route normalizes input server-side before persistence",reservations.indexOf('normalizeStorefrontReservationInput(body)')<reservations.indexOf('createPublicReservation(normalized.input)'));
check("reservation bridge runs in Next after callback",reservations.includes('after(async ()')&&reservations.includes('bridgePublicReservationToCashierFailOpen'));
check("reservation cancellation sync only runs for cancelled",reservationStatus.includes('result.data?.status === "cancelled"'));
check("paid or issued cashier order cannot be cancelled by reservation",pilot.includes('existing.status === "paid" || existing.status === "issued"'));
check("cancellation reuses existing legacy order status flow",pilot.includes('updateOrderStatus(existing.id, "cancelled")'));
check("cancellation requeues latest order snapshot",pilot.includes("queueStorefrontCommerceMirrorFailOpen(reservation.businessSlug, updated.data)"));
check("public products API requires businessSlug",publicProducts.includes('Hiányzik az üzlet azonosítója.'));
check("public products API exposes pilot catalog",publicProducts.includes('getStorefrontPilotCatalog'));
check("database repository implements async product loading",databaseRepo.includes('async listProducts(): Promise<AruterProduct[]>'));
check("database product query includes authoritative pricing and stock",databaseRepo.includes('price_net,vat_rate,stock_quantity,is_public_offer,is_active'));
check("database product mapping includes category and storage zone",databaseRepo.includes('categoryMap')&&databaseRepo.includes('zoneMap'));
check("AruterProduct supports public offer flag",types.includes('isPublicOffer?: boolean'));
check("legacy product GET awaits async repository",productRoute.includes('data: await getAruterRepository().listProducts()'));
check("legacy order GET awaits async repository",orderRoute.includes('data: await getAruterRepository().listOrders()'));
check("legacy event GET awaits async repository",eventsRoute.includes('data: await getAruterRepository().listEvents()'));
check("public UI loads pilot products from public API",ui.includes('/api/aruter/public-products?businessSlug='));
check("UI uses pilot result products without demo fallback when enabled",ui.includes('result.data.pilotEnabled ? result.data.products : business.products'));
check("UI explicitly displays empty pilot catalog",ui.includes('A pilot katalógusban jelenleg nincs foglalható termék.'));
check("out-of-stock product cannot be reserved in UI",ui.includes('disabled={unavailable}')&&ui.includes('product.stockStatus === "out_of_stock"'));
check("UI visibly identifies pilot catalog",ui.includes('Pilot katalógus · pénztári törzsadatból'));
check("UI visibly identifies cashier bridge when enabled",ui.includes('Pénztári bridge aktív'));
check("env example documents storefront pilot disabled by default",envExample.includes("ARUTER_STOREFRONT_PILOT_ENABLED=0"));
check("env example documents cashier bridge disabled by default",envExample.includes("ARUTER_STOREFRONT_ORDER_BRIDGE_ENABLED=0"));
check("env example documents optional pilot template",envExample.includes("ARUTER_STOREFRONT_PILOT_TEMPLATE="));

check("storefront Commerce queue is explicit opt-in",pilot.includes("ARUTER_STOREFRONT_COMMERCE_QUEUE_ENABLED")&&pilot.includes('=== "1"'));
check("queue tenant comes only from server environment",pilot.includes("ARUTER_STOREFRONT_COMMERCE_ORGANIZATION_ID")&&!pilot.includes("searchParams.get(\"organizationId\")"));
check("queue business slug is exact-match server configuration",pilot.includes("businessSlug !== configuredSlug"));
check("queue validates configured organization UUID",pilot.includes("[0-9a-f]{8}-[0-9a-f]{4}"));
check("queue uses service repository enqueue function",pilot.includes("enqueueCommerceMirrorAttemptForOrganization"));
check("queue failure remains fail-open",pilot.includes("QUEUE_FAILED_FAIL_OPEN"));
check("env example keeps Commerce queue disabled by default",envExample.includes("ARUTER_STOREFRONT_COMMERCE_QUEUE_ENABLED=0"));
check("env example documents trusted business slug mapping",envExample.includes("ARUTER_STOREFRONT_COMMERCE_BUSINESS_SLUG="));
check("env example documents trusted organization mapping",envExample.includes("ARUTER_STOREFRONT_COMMERCE_ORGANIZATION_ID="));

const failed=checks.filter((item)=>!item.condition);
console.log(`RESULT ${checks.length-failed.length}/${checks.length} PASS`);
assert.equal(failed.length,0,failed.map((item)=>item.name).join(", "));
